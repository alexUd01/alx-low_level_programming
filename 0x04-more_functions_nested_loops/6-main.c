#include "main.h"

/**
 * main - tests if "print_line" function is working perfectly
 *
 * Return: Always returns 0
 */
int main(void)
{
	print_line(0);
	print_line(2);
	print_line(10);
	print_line(-4);

	return (0);
}
