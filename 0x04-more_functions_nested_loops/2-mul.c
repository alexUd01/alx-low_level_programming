#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: the first parameter (integer 1)
 * @b: the second parameter (integer 2)
 *
 * Return: returns m
 */
int mul(int a, int b)
{
	int m;

	m = a * b;

	return (m);
}
